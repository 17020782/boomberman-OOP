package Interface;import java.awt.event.KeyEvent;import Map.Manager;import Object.BomberMan;public class InputManager {    /**     * class này dùng để xử lí khi nhận sự kiện từ bàn phím     */    private Manager manager;    private GamePanel gamePanel;    public InputManager(GamePanel gamePanel, Manager manager){        this.manager = manager;        this.gamePanel = gamePanel;    }    /**     * hàm xử lí sự kiện nhấn phím     * @param keyCode     */    public void processKeyPreesed(int keyCode){        switch (keyCode){            case KeyEvent.VK_UP:                gamePanel.bomberMan.setRunning(true);                gamePanel.bomberMan.setStanding(false);                gamePanel.bomberMan.setDirection(BomberMan.DIR_UP);                gamePanel.bomberMan.setSpeedUpDown(-1);                gamePanel.bomberMan.setSpeedRightLef(0);                //System.out.println(gamePanel.bomberMan.getPosX() + " " + gamePanel.bomberMan.getPosY());                break;            case KeyEvent.VK_DOWN:                gamePanel.bomberMan.setRunning(true);                gamePanel.bomberMan.setStanding(false);                gamePanel.bomberMan.setDirection(BomberMan.DIR_DOWN);                gamePanel.bomberMan.setSpeedUpDown(1);                gamePanel.bomberMan.setSpeedRightLef(0);                break;            case KeyEvent.VK_LEFT:                gamePanel.bomberMan.setRunning(true);                gamePanel.bomberMan.setStanding(false);                gamePanel.bomberMan.setDirection(BomberMan.DIR_LEFT);                gamePanel.bomberMan.setSpeedRightLef(-1);                gamePanel.bomberMan.setSpeedUpDown(0);                break;            case KeyEvent.VK_RIGHT:                gamePanel.bomberMan.setRunning(true);                gamePanel.bomberMan.setStanding(false);                gamePanel.bomberMan.setDirection(BomberMan.DIR_RIGHT);                gamePanel.bomberMan.setSpeedRightLef(1);                gamePanel.bomberMan.setSpeedUpDown(0);                break;            case KeyEvent.VK_ENTER:                break;            case KeyEvent.VK_SPACE:                if(gamePanel.bomberMan.isSetBom() == true) break;                if(gamePanel.bomberMan.isIgnite()==true) break;                else {                    //System.out.println(gamePanel.bomberMan.isSetBom());                    gamePanel.bomberMan.setTime(System.nanoTime());                    gamePanel.bomberMan.setSetBom(true);                    if(gamePanel.bomberMan.impactionBomberBox()) {                        gamePanel.bomberMan.setPosXBom(((gamePanel.bomberMan.getPosX()+gamePanel.bomberMan.getWidth()/2)/45)*45);                        gamePanel.bomberMan.setPosYBom(((gamePanel.bomberMan.getPosY()+gamePanel.bomberMan.getHeight())/45)*45);                    }                    else {                        gamePanel.bomberMan.setPosXBom(((gamePanel.bomberMan.getPosX() + gamePanel.bomberMan.getWidth() / 2) / 45) * 45);                        gamePanel.bomberMan.setPosYBom(((gamePanel.bomberMan.getPosY() + gamePanel.bomberMan.getHeight() * 2 / 3) / 45) * 45);                    }                    gamePanel.bomberMan.flashbom();                    break;                }        }    }    /**     * hàm xử lí sự kiện nhả phím     * @param keyCode     */    public void processKeyReleased(int keyCode){        switch (keyCode){            case KeyEvent.VK_UP:                gamePanel.bomberMan.setDirection(BomberMan.DIR_UP);                gamePanel.bomberMan.setRunning(false);                gamePanel.bomberMan.setStanding(true);                gamePanel.bomberMan.setSpeedUpDown(0);                gamePanel.bomberMan.setSpeedRightLef(0);                break;            case KeyEvent.VK_DOWN:                gamePanel.bomberMan.setDirection(BomberMan.DIR_DOWN);                gamePanel.bomberMan.setRunning(false);                gamePanel.bomberMan.setStanding(true);                gamePanel.bomberMan.setSpeedUpDown(0);                gamePanel.bomberMan.setSpeedRightLef(0);                break;            case KeyEvent.VK_LEFT:                gamePanel.bomberMan.setDirection(BomberMan.DIR_LEFT);                gamePanel.bomberMan.setRunning(false);                gamePanel.bomberMan.setStanding(true);                gamePanel.bomberMan.setSpeedUpDown(0);                gamePanel.bomberMan.setSpeedRightLef(0);                break;            case KeyEvent.VK_RIGHT:                gamePanel.bomberMan.setDirection(BomberMan.DIR_RIGHT);                gamePanel.bomberMan.setRunning(false);                gamePanel.bomberMan.setStanding(true);                gamePanel.bomberMan.setSpeedUpDown(0);                gamePanel.bomberMan.setSpeedRightLef(0);                break;            case KeyEvent.VK_ENTER:                break;            case KeyEvent.VK_BACK_SPACE:                break;        }    }}